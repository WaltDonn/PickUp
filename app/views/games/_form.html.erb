<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.datetime_field :time, id: :game_time %>
  </div>

  <div class="field">
    <%= form.label :max_size %>
    <%= form.number_field :max_size, id: :game_max_size %>
  </div>

  <div class="field">
    <%= form.label :curr_size %>
    <%= form.number_field :curr_size, id: :game_curr_size %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :game_description %>
  </div>

  <div class="field">
    <%= form.label :location_id %>
    <%= form.number_field :location_id, id: :game_location_id %>
  </div>

  <div class="field">
    <%= form.label :sport_id %>
    <%= form.number_field :sport_id, id: :game_sport_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
