<p id="notice"><%= notice %></p>

<h1>Friendship Invitations</h1>

<table>
  <thead>
    <tr>
      <th>Inviteeid</th>
      <th>Inviterid</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @friendship_invitations.each do |friendship_invitation| %>
      <tr>
        <td><%= friendship_invitation.inviteeID %></td>
        <td><%= friendship_invitation.inviterID %></td>
        <td><%= link_to 'Show', friendship_invitation %></td>
        <td><%= link_to 'Edit', edit_friendship_invitation_path(friendship_invitation) %></td>
        <td><%= link_to 'Destroy', friendship_invitation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Friendship Invitation', new_friendship_invitation_path %>
